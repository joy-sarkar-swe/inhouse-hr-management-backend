/**
 * @fileoverview Public barrel for all reusable NestJS interceptors.
 *
 * Import from here — never deep-import individual interceptor files.
 *
 * @example
 *   import { ResponseInterceptor } from 'src/common/interceptor';
 */
export { ResponseInterceptor } from './response.interceptor';
